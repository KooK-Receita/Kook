import { Card, Col, Navbar } from "react-bootstrap";
import "./header.css";

function Header() {
  return (
    <Navbar bg="primary" variant="light">
        bilu bilu
    </Navbar>
  );
}

export default Header;
